<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=true"></script>
    <%= javascript_include_tag "gmaps" %>
    <title>CanchitApp</title>
    <style type="text/css">
      html, body, #map {
        display: block;
        width: 100%;
        height: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= yield %>
    <script type="text/javascript">
      var map = new GMaps({
        div: '#map',
        lat: <%= @latitude %>,
        lng: <%= @longitude %>,
        enableNewStyle: true,
        disableDefaultUI: true,
        zoomControl: true,
        zoom: 17
      });

      map.addMarker({
        lat: <%= @latitude %>,
        lng: <%= @longitude %>,
        infoWindow: {
          content: '<p>¡Estás aquí!</p>'
        }
      });
    </script>
  </body>
</html>